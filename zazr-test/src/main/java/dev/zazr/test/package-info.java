/**
 * Property checks: {@link dev.zazr.test.Gen} generates values, {@link dev.zazr.test.Check} checks a
 * property against them and returns a {@link dev.zazr.test.CheckResult}, configured by a
 * {@link dev.zazr.test.CheckConfig}. A check is a method call, so it runs in any test framework.
 */
package dev.zazr.test;
