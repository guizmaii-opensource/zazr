<!-- Generated by scripts/check-complexity.scala (make docs-complexity). Do not edit. -->

| Operation | Cost | Note |
|---|---|---|
| `contains` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1): one hash lookup.">effectively O(1)</abbr> | effectively O(1): one hash lookup. |
| `add` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1): one hash lookup; a new element also costs a copy of a few small arrays, the rest is shared with this set.">effectively O(1)</abbr> | effectively O(1): one hash lookup; a new element also costs a copy of a few small arrays, the rest is shared with this set. |
| `remove` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1): one hash lookup, then a copy of a few small arrays; the rest is shared with this set.">effectively O(1)</abbr> | effectively O(1): one hash lookup, then a copy of a few small arrays; the rest is shared with this set. |
| `union` | <abbr class="cx cx-linear" title="O(m) for a set of m elements, each an effectively O(1) insertion; this set, or a HashSet argument, is returned as is when the other side is empty.">O(m)</abbr> | O(m) for a set of m elements, each an effectively O(1) insertion; this set, or a HashSet argument, is returned as is when the other side is empty. |
| `intersect` | <abbr class="cx cx-linear" title="O(m) for a set of m elements; O(min(n, m)) when it is a HashSet: the elements of the smaller set are checked against the larger one. Another kind of set is first copied into a hash set.">O(m)</abbr> | O(m) for a set of m elements; O(min(n, m)) when it is a HashSet: the elements of the smaller set are checked against the larger one. Another kind of set is first copied into a hash set. |
| `diff` | <abbr class="cx cx-linear" title="O(n + m) for a set of m elements; O(n) when it is a HashSet. The m elements are put in a hash set (unless they already are one), then every element of this set is checked against it: the whole set is walked even to remove one element, which remove does in effectively O(1).">O(n + m)</abbr> | O(n + m) for a set of m elements; O(n) when it is a HashSet. The m elements are put in a hash set (unless they already are one), then every element of this set is checked against it: the whole set is walked even to remove one element, which remove does in effectively O(1). |
| `min` | <abbr class="cx cx-linear" title="O(n): every element is compared in its natural order. On a TreeSet, head() gives the least element in the set's own order in O(log n).">O(n)</abbr> | O(n): every element is compared in its natural order. On a TreeSet, head() gives the least element in the set's own order in O(log n). |
| `max` | <abbr class="cx cx-linear" title="O(n): every element is compared in its natural order. On a TreeSet, last() gives the greatest element in the set's own order in O(log n).">O(n)</abbr> | O(n): every element is compared in its natural order. On a TreeSet, last() gives the greatest element in the set's own order in O(log n). |
