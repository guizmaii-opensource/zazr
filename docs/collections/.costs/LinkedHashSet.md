<!-- Generated by scripts/check-complexity.scala (make docs-complexity). Do not edit. -->

| Operation | Cost | Note |
|---|---|---|
| `contains` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1): one hash lookup.">effectively O(1)</abbr> | effectively O(1): one hash lookup. |
| `add` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1): one hash lookup; a new element is also inserted and added at the end of the insertion order.">effectively O(1)</abbr> | effectively O(1): one hash lookup; a new element is also inserted and added at the end of the insertion order. |
| `remove` | <abbr class="cx cx-amortisedconstant" title="amortised O(1); a single call can cost O(n). The removed element makes a gap in the insertion order, and the whole set is rebuilt in O(n) once the gaps outnumber the elements; removing the first or the last element also walks past the gaps next to it. Over a chain of removals, each on the result of the previous one, this averages to effectively O(1); removing again from an older version can pay O(n) every time.">amortised O(1)</abbr> | amortised O(1); a single call can cost O(n). The removed element makes a gap in the insertion order, and the whole set is rebuilt in O(n) once the gaps outnumber the elements; removing the first or the last element also walks past the gaps next to it. Over a chain of removals, each on the result of the previous one, this averages to effectively O(1); removing again from an older version can pay O(n) every time. |
| `union` | <abbr class="cx cx-linear" title="O(m) for a set of m elements, each an effectively O(1) add; this set, or a LinkedHashSet argument, is returned as is when the other side is empty.">O(m)</abbr> | O(m) for a set of m elements, each an effectively O(1) add; this set, or a LinkedHashSet argument, is returned as is when the other side is empty. |
| `intersect` | <abbr class="cx cx-linear" title="O(n + m) for a set of m elements; O(n) when it is a HashSet. The m elements are put in a hash set (unless they already are one), then every element of this set is checked against it and the kept ones are put in a new set.">O(n + m)</abbr> | O(n + m) for a set of m elements; O(n) when it is a HashSet. The m elements are put in a hash set (unless they already are one), then every element of this set is checked against it and the kept ones are put in a new set. |
| `diff` | <abbr class="cx cx-linear" title="O(n + m) for a set of m elements; O(n) when it is a HashSet. The m elements are put in a hash set (unless they already are one), then every element of this set is checked against it and the kept ones are put in a new set: the whole set is walked even to remove one element, which remove does in amortised O(1).">O(n + m)</abbr> | O(n + m) for a set of m elements; O(n) when it is a HashSet. The m elements are put in a hash set (unless they already are one), then every element of this set is checked against it and the kept ones are put in a new set: the whole set is walked even to remove one element, which remove does in amortised O(1). |
| `min` | <abbr class="cx cx-linear" title="O(n): every element is compared in its natural order. On a TreeSet, head() gives the least element in the set's own order in O(log n).">O(n)</abbr> | O(n): every element is compared in its natural order. On a TreeSet, head() gives the least element in the set's own order in O(log n). |
| `max` | <abbr class="cx cx-linear" title="O(n): every element is compared in its natural order. On a TreeSet, last() gives the greatest element in the set's own order in O(log n).">O(n)</abbr> | O(n): every element is compared in its natural order. On a TreeSet, last() gives the greatest element in the set's own order in O(log n). |
| `head` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1): the first element of the insertion order.">effectively O(1)</abbr> | effectively O(1): the first element of the insertion order. |
| `take` | <abbr class="cx cx-linear" title="O(n); O(min(k, n - k)) for k kept elements when nothing has been removed: the smaller side, kept or dropped, is added to or taken out of a hash map, and the rest is shared. After removals, finding the cut walks past the gaps left by removed elements, and the result is rebuilt when it holds more gaps than elements.">O(n)</abbr> | O(n); O(min(k, n - k)) for k kept elements when nothing has been removed: the smaller side, kept or dropped, is added to or taken out of a hash map, and the rest is shared. After removals, finding the cut walks past the gaps left by removed elements, and the result is rebuilt when it holds more gaps than elements. |
| `drop` | <abbr class="cx cx-linear" title="O(n); O(min(k, n - k)) for k dropped elements on a set with no earlier removals, as take.">O(n)</abbr> | O(n); O(min(k, n - k)) for k dropped elements on a set with no earlier removals, as take. |
