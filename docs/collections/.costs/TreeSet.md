<!-- Generated by scripts/check-complexity.scala (make docs-complexity). Do not edit. -->

| Operation | Cost | Note |
|---|---|---|
| `contains` | <abbr class="cx cx-logarithmic" title="O(log n): one walk down the tree, comparing with the comparator, not with equals.">O(log n)</abbr> | O(log n): one walk down the tree, comparing with the comparator, not with equals. |
| `add` | <abbr class="cx cx-logarithmic" title="O(log n): one lookup, then one insertion when the element is new.">O(log n)</abbr> | O(log n): one lookup, then one insertion when the element is new. |
| `remove` | <abbr class="cx cx-logarithmic" title="O(log n): one deletion from the tree.">O(log n)</abbr> | O(log n): one deletion from the tree. |
| `union` | <abbr class="cx cx-linearithmic" title="O(m log(n + m)) for a set of m elements: one lookup each, then one insertion for each new one. When elements is a TreeSet with an equal comparator (for a lambda, the same object), the cost is O(m log(n / m + 1)), m then being the smaller of the two sizes: the two trees are cut and joined, not rebuilt.">O(m log(n + m))</abbr> | O(m log(n + m)) for a set of m elements: one lookup each, then one insertion for each new one. When elements is a TreeSet with an equal comparator (for a lambda, the same object), the cost is O(m log(n / m + 1)), m then being the smaller of the two sizes: the two trees are cut and joined, not rebuilt. |
| `intersect` | <abbr class="cx cx-linearithmic" title="O(m + n log n) for a set of m elements: they are put in a hash set, then the kept elements are inserted one by one into a new tree. When elements is a TreeSet with an equal comparator (for a lambda, the same object), the cost is O(m log(n / m + 1)), m then being the smaller of the two sizes: the two trees are cut and joined, not rebuilt.">O(m + n log n)</abbr> | O(m + n log n) for a set of m elements: they are put in a hash set, then the kept elements are inserted one by one into a new tree. When elements is a TreeSet with an equal comparator (for a lambda, the same object), the cost is O(m log(n / m + 1)), m then being the smaller of the two sizes: the two trees are cut and joined, not rebuilt. |
| `diff` | <abbr class="cx cx-linearithmic" title="O(m + n log n) for a set of m elements: they are put in a hash set, then the kept elements are inserted one by one into a new tree. When elements is a TreeSet with an equal comparator (for a lambda, the same object), the cost is O(m log(n / m + 1)), m then being the smaller of the two sizes: the two trees are cut and joined, not rebuilt.">O(m + n log n)</abbr> | O(m + n log n) for a set of m elements: they are put in a hash set, then the kept elements are inserted one by one into a new tree. When elements is a TreeSet with an equal comparator (for a lambda, the same object), the cost is O(m log(n / m + 1)), m then being the smaller of the two sizes: the two trees are cut and joined, not rebuilt. |
| `min` | <abbr class="cx cx-linear" title="O(n): every element is compared in its natural order. On a TreeSet, head() gives the least element in the set's own order in O(log n).">O(n)</abbr> | O(n): every element is compared in its natural order. On a TreeSet, head() gives the least element in the set's own order in O(log n). |
| `max` | <abbr class="cx cx-linear" title="O(n): every element is compared in its natural order. On a TreeSet, last() gives the greatest element in the set's own order in O(log n).">O(n)</abbr> | O(n): every element is compared in its natural order. On a TreeSet, last() gives the greatest element in the set's own order in O(log n). |
| `head` | <abbr class="cx cx-logarithmic" title="O(log n): the smallest element is found by walking down the tree, with no comparison.">O(log n)</abbr> | O(log n): the smallest element is found by walking down the tree, with no comparison. |
| `take` | <abbr class="cx cx-logarithmic" title="O(log n): the tree is cut at that position without visiting the elements, and the result shares the rest of the tree.">O(log n)</abbr> | O(log n): the tree is cut at that position without visiting the elements, and the result shares the rest of the tree. |
| `drop` | <abbr class="cx cx-logarithmic" title="O(log n): the tree is cut at that position without visiting the elements, and the result shares the rest of the tree.">O(log n)</abbr> | O(log n): the tree is cut at that position without visiting the elements, and the result shares the rest of the tree. |
