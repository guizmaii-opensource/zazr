<!-- Generated by scripts/check-complexity.scala (make docs-complexity). Do not edit. -->

| Operation | Cost | Note |
|---|---|---|
| `head` | <abbr class="cx cx-constant" title="O(1) (the first leaf is a field of the tree).">O(1)</abbr> | O(1) (the first leaf is a field of the tree). |
| `tail` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1): the first leaf, at most 32 elements, is copied without its first element; the arrays above it change only when that leaf held one element (amortised O(1)).">effectively O(1)</abbr> | effectively O(1): the first leaf, at most 32 elements, is copied without its first element; the arrays above it change only when that leaf held one element (amortised O(1)). |
| `last` | <abbr class="cx cx-constant" title="O(1) (the last leaf is a field of the tree).">O(1)</abbr> | O(1) (the last leaf is a field of the tree). |
| `init` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1): the last leaf, at most 32 elements, is copied without its last element; the arrays above it change only when that leaf held one element (amortised O(1)).">effectively O(1)</abbr> | effectively O(1): the last leaf, at most 32 elements, is copied without its last element; the arrays above it change only when that leaf held one element (amortised O(1)). |
| `get` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1) (O(log32 n) array reads; one read in the first and the last leaf).">effectively O(1)</abbr> | effectively O(1) (O(log32 n) array reads; one read in the first and the last leaf). |
| `update` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1) (the leaf holding the element is copied, with the arrays above it, at most six arrays of 32 entries; only the leaf itself in the first and the last leaf).">effectively O(1)</abbr> | effectively O(1) (the leaf holding the element is copied, with the arrays above it, at most six arrays of 32 entries; only the leaf itself in the first and the last leaf). |
| `prepend` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1): the first leaf, at most 32 elements, is copied, and the arrays above it only once it is full, once every 32 prepends (amortised O(1)).">effectively O(1)</abbr> | effectively O(1): the first leaf, at most 32 elements, is copied, and the arrays above it only once it is full, once every 32 prepends (amortised O(1)). |
| `append` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1): the last leaf, at most 32 elements, is copied, and the arrays above it only once it is full, once every 32 appends (amortised O(1)).">effectively O(1)</abbr> | effectively O(1): the last leaf, at most 32 elements, is copied, and the arrays above it only once it is full, once every 32 appends (amortised O(1)). |
| `prependAll` | <abbr class="cx cx-linear" title="O(m) for m prepended elements; O(min(n, m)) when iterable is a Vector: the arrays of the longer side are reused whole and only the shorter side is copied, element by element or leaf by leaf, plus O(log n) arrays. O(1) when this Vector is empty and iterable is a Vector, which is returned as is.">O(m)</abbr> | O(m) for m prepended elements; O(min(n, m)) when iterable is a Vector: the arrays of the longer side are reused whole and only the shorter side is copied, element by element or leaf by leaf, plus O(log n) arrays. O(1) when this Vector is empty and iterable is a Vector, which is returned as is. |
| `appendAll` | <abbr class="cx cx-linear" title="O(m) for m appended elements; O(min(n, m)) when iterable is a Vector: the arrays of the longer side are reused whole and only the shorter side is copied, element by element or leaf by leaf, plus O(log n) arrays. O(1) when this Vector is empty and iterable is a Vector, which is returned as is.">O(m)</abbr> | O(m) for m appended elements; O(min(n, m)) when iterable is a Vector: the arrays of the longer side are reused whole and only the shorter side is copied, element by element or leaf by leaf, plus O(log n) arrays. O(1) when this Vector is empty and iterable is a Vector, which is returned as is. |
| `insert` | <abbr class="cx cx-linear" title="O(min(i, n - i)): the shorter side is added to the longer one, element by element or leaf by leaf.">O(min(i, n - i))</abbr> | O(min(i, n - i)): the shorter side is added to the longer one, element by element or leaf by leaf. |
| `removeAt` | <abbr class="cx cx-linear" title="O(min(i, n - i)): the shorter side is added to the longer one, element by element or leaf by leaf.">O(min(i, n - i))</abbr> | O(min(i, n - i)): the shorter side is added to the longer one, element by element or leaf by leaf. |
| `take` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1) (the arrays along the cut are copied, O(log32 n) of them; the others are shared).">effectively O(1)</abbr> | effectively O(1) (the arrays along the cut are copied, O(log32 n) of them; the others are shared). |
| `drop` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1) (the arrays along the cut are copied, O(log32 n) of them; the others are shared).">effectively O(1)</abbr> | effectively O(1) (the arrays along the cut are copied, O(log32 n) of them; the others are shared). |
| `slice` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1) (the arrays along the two cuts are copied, O(log32 n) of them; the others are shared).">effectively O(1)</abbr> | effectively O(1) (the arrays along the two cuts are copied, O(log32 n) of them; the others are shared). |
| `splitAt(Predicate<? super T>)` | <abbr class="cx cx-linear" title="O(k) for k elements before the split, then an effectively O(1) split.">O(k)</abbr> | O(k) for k elements before the split, then an effectively O(1) split. |
| `splitAt(int)` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1).">effectively O(1)</abbr> | effectively O(1). |
| `reverse` | <abbr class="cx cx-linear" title="O(n).">O(n)</abbr> | O(n). |
| `sorted` | <abbr class="cx cx-linearithmic" title="O(n log n) comparisons; the elements are copied to an array, sorted there and copied into leaves.">O(n log n)</abbr> | O(n log n) comparisons; the elements are copied to an array, sorted there and copied into leaves. |
| `length` | <abbr class="cx cx-constant" title="O(1); the length is a field of the tree.">O(1)</abbr> | O(1); the length is a field of the tree. |
| `contains` | <abbr class="cx cx-linear" title="O(n) for this default, which walks the elements; the sets and the maps override it with their own lookup.">O(n)</abbr> | O(n) for this default, which walks the elements; the sets and the maps override it with their own lookup. |
| `indexOf` | <abbr class="cx cx-linear" title="O(n).">O(n)</abbr> | O(n). |
| `zip` | <abbr class="cx cx-linear" title="O(min(n, m)) for m elements of that.">O(min(n, m))</abbr> | O(min(n, m)) for m elements of that. |
| `sliding(int)` | <abbr class="cx cx-linear" title="O(n) windows, each an effectively O(1) slice sharing this Vector's leaves.">O(n)</abbr> | O(n) windows, each an effectively O(1) slice sharing this Vector's leaves. |
| `sliding(int, int)` | <abbr class="cx cx-linear" title="O(n / step) windows, each an effectively O(1) slice sharing this Vector's leaves.">O(n / step)</abbr> | O(n / step) windows, each an effectively O(1) slice sharing this Vector's leaves. |
| `grouped` | <abbr class="cx cx-linear" title="O(n / size) blocks, each an effectively O(1) slice sharing this Vector's leaves.">O(n / size)</abbr> | O(n / size) blocks, each an effectively O(1) slice sharing this Vector's leaves. |
| `distinct` | <abbr class="cx cx-linear" title="O(n).">O(n)</abbr> | O(n). |
