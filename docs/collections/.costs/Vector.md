<!-- Generated by scripts/check-complexity.scala (make docs-complexity). Do not edit. -->

| Operation | Cost | Note |
|---|---|---|
| `head` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1).">effectively O(1)</abbr> | effectively O(1). |
| `tail` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1): the result shares its elements with this Vector; only a few small arrays at the cut are copied.">effectively O(1)</abbr> | effectively O(1): the result shares its elements with this Vector; only a few small arrays at the cut are copied. |
| `last` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1).">effectively O(1)</abbr> | effectively O(1). |
| `init` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1): the result shares its elements with this Vector; only a few small arrays at the cut are copied.">effectively O(1)</abbr> | effectively O(1): the result shares its elements with this Vector; only a few small arrays at the cut are copied. |
| `get` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1): at most six array lookups, whatever the size.">effectively O(1)</abbr> | effectively O(1): at most six array lookups, whatever the size. |
| `update` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1): copies a few small arrays of 32 elements, not the Vector. On a Vector of primitive values, a value of another class first converts every element: O(n), paid again at each such write on the same Vector (see the class documentation).">effectively O(1)</abbr> | effectively O(1): copies a few small arrays of 32 elements, not the Vector. On a Vector of primitive values, a value of another class first converts every element: O(n), paid again at each such write on the same Vector (see the class documentation). |
| `prepend` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1): copies a few small arrays of 32 elements, not the Vector. On a Vector of primitive values, a value of another class first converts every element: O(n), paid again at each such write on the same Vector (see the class documentation).">effectively O(1)</abbr> | effectively O(1): copies a few small arrays of 32 elements, not the Vector. On a Vector of primitive values, a value of another class first converts every element: O(n), paid again at each such write on the same Vector (see the class documentation). |
| `append` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1): copies a few small arrays of 32 elements, not the Vector. On a Vector of primitive values, a value of another class first converts every element: O(n), paid again at each such write on the same Vector (see the class documentation).">effectively O(1)</abbr> | effectively O(1): copies a few small arrays of 32 elements, not the Vector. On a Vector of primitive values, a value of another class first converts every element: O(n), paid again at each such write on the same Vector (see the class documentation). |
| `prependAll` | <abbr class="cx cx-linear" title="O(m) for m prepended elements, even when this Vector is much shorter than the argument; the elements of this Vector are shared, not copied. O(1) when this Vector is empty and iterable is a Vector, which is returned as is. On a Vector of primitive values, a value of another class first converts every element: O(n), paid again at each such write on the same Vector (see the class documentation).">O(m)</abbr> | O(m) for m prepended elements, even when this Vector is much shorter than the argument; the elements of this Vector are shared, not copied. O(1) when this Vector is empty and iterable is a Vector, which is returned as is. On a Vector of primitive values, a value of another class first converts every element: O(n), paid again at each such write on the same Vector (see the class documentation). |
| `appendAll` | <abbr class="cx cx-linear" title="O(m) for m appended elements, even when this Vector is much shorter than the argument; the elements of this Vector are shared, not copied. O(1) when this Vector is empty and iterable is a Vector, which is returned as is. On a Vector of primitive values, a value of another class first converts every element: O(n), paid again at each such write on the same Vector (see the class documentation).">O(m)</abbr> | O(m) for m appended elements, even when this Vector is much shorter than the argument; the elements of this Vector are shared, not copied. O(1) when this Vector is empty and iterable is a Vector, which is returned as is. On a Vector of primitive values, a value of another class first converts every element: O(n), paid again at each such write on the same Vector (see the class documentation). |
| `insert` | <abbr class="cx cx-linear" title="O(min(i, n - i)): the elements on the shorter side of i are copied, those on the longer side are shared. On a Vector of primitive values, a value of another class first converts every element: O(n), paid again at each such write on the same Vector (see the class documentation).">O(min(i, n - i))</abbr> | O(min(i, n - i)): the elements on the shorter side of i are copied, those on the longer side are shared. On a Vector of primitive values, a value of another class first converts every element: O(n), paid again at each such write on the same Vector (see the class documentation). |
| `removeAt` | <abbr class="cx cx-linear" title="O(min(i, n - i)): the elements on the shorter side of i are copied, those on the longer side are shared.">O(min(i, n - i))</abbr> | O(min(i, n - i)): the elements on the shorter side of i are copied, those on the longer side are shared. |
| `take` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1): the result shares its elements with this Vector; only a few small arrays at the cut are copied.">effectively O(1)</abbr> | effectively O(1): the result shares its elements with this Vector; only a few small arrays at the cut are copied. |
| `drop` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1): the result shares its elements with this Vector; only a few small arrays at the cut are copied.">effectively O(1)</abbr> | effectively O(1): the result shares its elements with this Vector; only a few small arrays at the cut are copied. |
| `slice` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1): the result shares its elements with this Vector; only a few small arrays at the cut are copied.">effectively O(1)</abbr> | effectively O(1): the result shares its elements with this Vector; only a few small arrays at the cut are copied. |
| `splitAt(Predicate<? super T>)` | <abbr class="cx cx-linear" title="O(k) for k elements before the split, then an effectively O(1) split.">O(k)</abbr> | O(k) for k elements before the split, then an effectively O(1) split. |
| `splitAt(int)` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1): one take and one drop; both parts share their elements with this Vector.">effectively O(1)</abbr> | effectively O(1): one take and one drop; both parts share their elements with this Vector. |
| `reverse` | <abbr class="cx cx-linear" title="O(n).">O(n)</abbr> | O(n). |
| `sorted` | <abbr class="cx cx-linearithmic" title="O(n log n) comparisons; the elements are copied to an array and sorted there.">O(n log n)</abbr> | O(n log n) comparisons; the elements are copied to an array and sorted there. |
| `length` | <abbr class="cx cx-constant" title="O(1): the length is stored.">O(1)</abbr> | O(1): the length is stored. |
| `contains` | <abbr class="cx cx-linear" title="O(n): the elements are compared one by one until an equal one is found. The sets and the maps override it with a lookup.">O(n)</abbr> | O(n): the elements are compared one by one until an equal one is found. The sets and the maps override it with a lookup. |
| `indexOf` | <abbr class="cx cx-linear" title="O(n).">O(n)</abbr> | O(n). |
| `zip` | <abbr class="cx cx-linear" title="O(min(n, m)) for m elements of that.">O(min(n, m))</abbr> | O(min(n, m)) for m elements of that. |
| `sliding(int)` | <abbr class="cx cx-linear" title="O(n): one effectively O(1) slice per window; each window shares its elements with this Vector, so no element is copied.">O(n)</abbr> | O(n): one effectively O(1) slice per window; each window shares its elements with this Vector, so no element is copied. |
| `sliding(int, int)` | <abbr class="cx cx-linear" title="O(n / step): one effectively O(1) slice per window; each window shares its elements with this Vector, so no element is copied.">O(n / step)</abbr> | O(n / step): one effectively O(1) slice per window; each window shares its elements with this Vector, so no element is copied. |
| `grouped` | <abbr class="cx cx-linear" title="O(n / size): one effectively O(1) slice per block; each block shares its elements with this Vector.">O(n / size)</abbr> | O(n / size): one effectively O(1) slice per block; each block shares its elements with this Vector. |
| `distinct` | <abbr class="cx cx-linear" title="O(n).">O(n)</abbr> | O(n). |
