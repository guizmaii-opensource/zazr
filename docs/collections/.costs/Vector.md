<!-- Generated by scripts/check-complexity.scala (make docs-complexity). Do not edit. -->

| Operation | Cost | Note |
|---|---|---|
| `head` | <abbr class="cx cx-constant" title="O(1): the first elements are kept apart, in their own array.">O(1)</abbr> | O(1): the first elements are kept apart, in their own array. |
| `tail` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1): the first array, at most 32 elements, is copied without its first element; the few arrays above it change only when it held one element.">effectively O(1)</abbr> | effectively O(1): the first array, at most 32 elements, is copied without its first element; the few arrays above it change only when it held one element. |
| `last` | <abbr class="cx cx-constant" title="O(1): the last elements are kept apart, in their own array.">O(1)</abbr> | O(1): the last elements are kept apart, in their own array. |
| `init` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1): the last array, at most 32 elements, is copied without its last element; the few arrays above it change only when it held one element.">effectively O(1)</abbr> | effectively O(1): the last array, at most 32 elements, is copied without its last element; the few arrays above it change only when it held one element. |
| `get` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1): at most six array reads, whatever the size; one for the first and the last 32 elements.">effectively O(1)</abbr> | effectively O(1): at most six array reads, whatever the size; one for the first and the last 32 elements. |
| `update` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1): the array of 32 elements that holds it is copied, with at most five arrays above it; the rest is shared.">effectively O(1)</abbr> | effectively O(1): the array of 32 elements that holds it is copied, with at most five arrays above it; the rest is shared. |
| `prepend` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1): the first array, at most 32 elements, is copied; once every 32 prepends, a few arrays above it are copied too.">effectively O(1)</abbr> | effectively O(1): the first array, at most 32 elements, is copied; once every 32 prepends, a few arrays above it are copied too. |
| `append` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1): the last array, at most 32 elements, is copied; once every 32 appends, a few arrays above it are copied too.">effectively O(1)</abbr> | effectively O(1): the last array, at most 32 elements, is copied; once every 32 appends, a few arrays above it are copied too. |
| `prependAll` | <abbr class="cx cx-linear" title="O(m) for m prepended elements; O(min(n, m)) when iterable is a Vector: the longer side is shared and only the shorter one is copied. O(1) when this Vector is empty and iterable is a Vector, which is returned as is.">O(m)</abbr> | O(m) for m prepended elements; O(min(n, m)) when iterable is a Vector: the longer side is shared and only the shorter one is copied. O(1) when this Vector is empty and iterable is a Vector, which is returned as is. |
| `appendAll` | <abbr class="cx cx-linear" title="O(m) for m appended elements; O(min(n, m)) when iterable is a Vector: the longer side is shared and only the shorter one is copied. O(1) when this Vector is empty and iterable is a Vector, which is returned as is.">O(m)</abbr> | O(m) for m appended elements; O(min(n, m)) when iterable is a Vector: the longer side is shared and only the shorter one is copied. O(1) when this Vector is empty and iterable is a Vector, which is returned as is. |
| `insert` | <abbr class="cx cx-linear" title="O(min(i, n - i)): the elements on the shorter side of i are copied, those on the longer side are shared.">O(min(i, n - i))</abbr> | O(min(i, n - i)): the elements on the shorter side of i are copied, those on the longer side are shared. |
| `removeAt` | <abbr class="cx cx-linear" title="O(min(i, n - i)): the elements on the shorter side of i are copied, those on the longer side are shared.">O(min(i, n - i))</abbr> | O(min(i, n - i)): the elements on the shorter side of i are copied, those on the longer side are shared. |
| `take` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1): the result shares its elements with this Vector; only the few small arrays along the cut are copied.">effectively O(1)</abbr> | effectively O(1): the result shares its elements with this Vector; only the few small arrays along the cut are copied. |
| `drop` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1): the result shares its elements with this Vector; only the few small arrays along the cut are copied.">effectively O(1)</abbr> | effectively O(1): the result shares its elements with this Vector; only the few small arrays along the cut are copied. |
| `slice` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1): the result shares its elements with this Vector; only the few small arrays along the cut are copied.">effectively O(1)</abbr> | effectively O(1): the result shares its elements with this Vector; only the few small arrays along the cut are copied. |
| `splitAt(Predicate<? super T>)` | <abbr class="cx cx-linear" title="O(k) for k elements before the split, then an effectively O(1) split.">O(k)</abbr> | O(k) for k elements before the split, then an effectively O(1) split. |
| `splitAt(int)` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1): one take and one drop; both parts share their elements with this Vector.">effectively O(1)</abbr> | effectively O(1): one take and one drop; both parts share their elements with this Vector. |
| `reverse` | <abbr class="cx cx-linear" title="O(n).">O(n)</abbr> | O(n). |
| `sorted` | <abbr class="cx cx-linearithmic" title="O(n log n) comparisons; the elements are copied to an array and sorted there.">O(n log n)</abbr> | O(n log n) comparisons; the elements are copied to an array and sorted there. |
| `size` | <abbr class="cx cx-constant" title="O(1): the size is stored.">O(1)</abbr> | O(1): the size is stored. |
| `contains` | <abbr class="cx cx-linear" title="O(n): the elements are compared one by one until an equal one is found. The sets and the maps override it with a lookup.">O(n)</abbr> | O(n): the elements are compared one by one until an equal one is found. The sets and the maps override it with a lookup. |
| `indexOf` | <abbr class="cx cx-linear" title="O(n).">O(n)</abbr> | O(n). |
| `zip` | <abbr class="cx cx-linear" title="O(min(n, m)) for m elements of that.">O(min(n, m))</abbr> | O(min(n, m)) for m elements of that. |
| `sliding(int)` | <abbr class="cx cx-linear" title="O(n): one effectively O(1) slice per window; each window shares its elements with this Vector, so no element is copied.">O(n)</abbr> | O(n): one effectively O(1) slice per window; each window shares its elements with this Vector, so no element is copied. |
| `sliding(int, int)` | <abbr class="cx cx-linear" title="O(n / step): one effectively O(1) slice per window; each window shares its elements with this Vector, so no element is copied.">O(n / step)</abbr> | O(n / step): one effectively O(1) slice per window; each window shares its elements with this Vector, so no element is copied. |
| `grouped` | <abbr class="cx cx-linear" title="O(n / size): one effectively O(1) slice per block; each block shares its elements with this Vector.">O(n / size)</abbr> | O(n / size): one effectively O(1) slice per block; each block shares its elements with this Vector. |
| `distinct` | <abbr class="cx cx-linear" title="O(n).">O(n)</abbr> | O(n). |
