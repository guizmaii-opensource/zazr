<!-- Generated by scripts/check-complexity.scala (make docs-complexity). Do not edit. -->

| Operation | Cost | Note |
|---|---|---|
| `head` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1).">effectively O(1)</abbr> | effectively O(1). |
| `tail` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1) (the path to the first leaf is trimmed).">effectively O(1)</abbr> | effectively O(1) (the path to the first leaf is trimmed). |
| `last` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1).">effectively O(1)</abbr> | effectively O(1). |
| `init` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1) (the path to the last leaf is trimmed).">effectively O(1)</abbr> | effectively O(1) (the path to the last leaf is trimmed). |
| `get` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1) (O(log32 n) trie access).">effectively O(1)</abbr> | effectively O(1) (O(log32 n) trie access). |
| `update` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1) (a path copy; the leaf holding the element is copied). On a Vector of primitive values, the first value of another class converts every element once, O(n) (see the class documentation).">effectively O(1)</abbr> | effectively O(1) (a path copy; the leaf holding the element is copied). On a Vector of primitive values, the first value of another class converts every element once, O(n) (see the class documentation). |
| `prepend` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1) (a path copy; the first leaf is copied). On a Vector of primitive values, the first value of another class converts every element once, O(n) (see the class documentation).">effectively O(1)</abbr> | effectively O(1) (a path copy; the first leaf is copied). On a Vector of primitive values, the first value of another class converts every element once, O(n) (see the class documentation). |
| `append` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1) (a path copy; the last leaf is copied). On a Vector of primitive values, the first value of another class converts every element once, O(n) (see the class documentation).">effectively O(1)</abbr> | effectively O(1) (a path copy; the last leaf is copied). On a Vector of primitive values, the first value of another class converts every element once, O(n) (see the class documentation). |
| `prependAll` | <abbr class="cx cx-linear" title="O(m) for m prepended elements (one leaf copy per 32 elements plus a path copy); O(1) when this Vector is empty and iterable is a Vector, which is returned as is. On a Vector of primitive values, the first value of another class converts every element once, O(n) (see the class documentation).">O(m)</abbr> | O(m) for m prepended elements (one leaf copy per 32 elements plus a path copy); O(1) when this Vector is empty and iterable is a Vector, which is returned as is. On a Vector of primitive values, the first value of another class converts every element once, O(n) (see the class documentation). |
| `appendAll` | <abbr class="cx cx-linear" title="O(m) for m appended elements (one leaf copy per 32 elements plus a path copy); O(1) when this Vector is empty and iterable is a Vector, which is returned as is. On a Vector of primitive values, the first value of another class converts every element once, O(n) (see the class documentation).">O(m)</abbr> | O(m) for m appended elements (one leaf copy per 32 elements plus a path copy); O(1) when this Vector is empty and iterable is a Vector, which is returned as is. On a Vector of primitive values, the first value of another class converts every element once, O(n) (see the class documentation). |
| `insert` | <abbr class="cx cx-linear" title="O(min(i, n - i)): the shorter side is re-appended or re-prepended element by element. On a Vector of primitive values, the first value of another class converts every element once, O(n) (see the class documentation).">O(min(i, n - i))</abbr> | O(min(i, n - i)): the shorter side is re-appended or re-prepended element by element. On a Vector of primitive values, the first value of another class converts every element once, O(n) (see the class documentation). |
| `removeAt` | <abbr class="cx cx-linear" title="O(min(i, n - i)): the shorter side is re-appended or re-prepended element by element.">O(min(i, n - i))</abbr> | O(min(i, n - i)): the shorter side is re-appended or re-prepended element by element. |
| `take` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1) (the path to the new last leaf is trimmed).">effectively O(1)</abbr> | effectively O(1) (the path to the new last leaf is trimmed). |
| `drop` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1) (the path to the new first leaf is trimmed).">effectively O(1)</abbr> | effectively O(1) (the path to the new first leaf is trimmed). |
| `slice` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1) (the paths to the new first and last leaves are trimmed).">effectively O(1)</abbr> | effectively O(1) (the paths to the new first and last leaves are trimmed). |
| `splitAt(Predicate<? super T>)` | <abbr class="cx cx-linear" title="O(k) for k elements before the split, then an effectively O(1) split.">O(k)</abbr> | O(k) for k elements before the split, then an effectively O(1) split. |
| `splitAt(int)` | <abbr class="cx cx-effectivelyconstant" title="effectively O(1).">effectively O(1)</abbr> | effectively O(1). |
| `reverse` | <abbr class="cx cx-linear" title="O(n).">O(n)</abbr> | O(n). |
| `sorted` | <abbr class="cx cx-linearithmic" title="O(n log n) comparisons; the elements are copied to an array, sorted there and regrouped into leaves.">O(n log n)</abbr> | O(n log n) comparisons; the elements are copied to an array, sorted there and regrouped into leaves. |
| `length` | <abbr class="cx cx-constant" title="O(1); the length is a field of the trie.">O(1)</abbr> | O(1); the length is a field of the trie. |
| `contains` | <abbr class="cx cx-linear" title="O(n) for this default, which walks the elements; the sets and the maps override it with their own lookup.">O(n)</abbr> | O(n) for this default, which walks the elements; the sets and the maps override it with their own lookup. |
| `indexOf` | <abbr class="cx cx-linear" title="O(n).">O(n)</abbr> | O(n). |
| `zip` | <abbr class="cx cx-linear" title="O(min(n, m)) for m elements of that.">O(min(n, m))</abbr> | O(min(n, m)) for m elements of that. |
| `sliding(int)` | <abbr class="cx cx-linear" title="O(n) windows, each an effectively O(1) slice sharing this Vector's leaves.">O(n)</abbr> | O(n) windows, each an effectively O(1) slice sharing this Vector's leaves. |
| `sliding(int, int)` | <abbr class="cx cx-linear" title="O(n / step) windows, each an effectively O(1) slice sharing this Vector's leaves.">O(n / step)</abbr> | O(n / step) windows, each an effectively O(1) slice sharing this Vector's leaves. |
| `grouped` | <abbr class="cx cx-linear" title="O(n / size) blocks, each an effectively O(1) slice sharing this Vector's leaves.">O(n / size)</abbr> | O(n / size) blocks, each an effectively O(1) slice sharing this Vector's leaves. |
| `distinct` | <abbr class="cx cx-linear" title="O(n).">O(n)</abbr> | O(n). |
