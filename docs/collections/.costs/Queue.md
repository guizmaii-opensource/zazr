<!-- Generated by scripts/check-complexity.scala (make docs-complexity). Do not edit. -->

| Operation | Cost | Note |
|---|---|---|
| `head` | <abbr class="cx cx-constant" title="O(1).">O(1)</abbr> | O(1). |
| `tail` | <abbr class="cx cx-amortisedconstant" title="amortised O(1) over a chain of calls, each on the result of the previous one. When the removed element is the last one before the elements held at the back, the call puts those in order in one O(n) step. Calling tail() again on the same older Queue pays that O(n) step each time.">amortised O(1)</abbr> | amortised O(1) over a chain of calls, each on the result of the previous one. When the removed element is the last one before the elements held at the back, the call puts those in order in one O(n) step. Calling tail() again on the same older Queue pays that O(n) step each time. |
| `last` | <abbr class="cx cx-linear" title="O(n); O(1) when some elements are held at the back, as after an enqueue.">O(n)</abbr> | O(n); O(1) when some elements are held at the back, as after an enqueue. |
| `init` | <abbr class="cx cx-amortisedconstant" title="amortised O(1) over a chain of calls, each on the result of the previous one. When no element is held at the back, the call moves the second half of the Queue there in one O(n) step, which the next calls use up. Calling init() again on the same older Queue pays that O(n) step each time.">amortised O(1)</abbr> | amortised O(1) over a chain of calls, each on the result of the previous one. When no element is held at the back, the call moves the second half of the Queue there in one O(n) step, which the next calls use up. Calling init() again on the same older Queue pays that O(n) step each time. |
| `get` | <abbr class="cx cx-linear" title="O(n); O(i) for an index i that falls before the elements held at the back. Every index does on a Queue built by ofAll; on a Queue built by enqueue, only index 0 does.">O(n)</abbr> | O(n); O(i) for an index i that falls before the elements held at the back. Every index does on a Queue built by ofAll; on a Queue built by enqueue, only index 0 does. |
| `update` | <abbr class="cx cx-linear" title="O(n).">O(n)</abbr> | O(n). |
| `prepend` | <abbr class="cx cx-constant" title="O(1).">O(1)</abbr> | O(1). |
| `append` | <abbr class="cx cx-constant" title="O(1), as enqueue.">O(1)</abbr> | O(1), as enqueue. |
| `prependAll` | <abbr class="cx cx-linear" title="O(m) for m prepended elements.">O(m)</abbr> | O(m) for m prepended elements. |
| `appendAll` | <abbr class="cx cx-linear" title="O(m) for m appended elements.">O(m)</abbr> | O(m) for m appended elements. |
| `insert` | <abbr class="cx cx-linear" title="O(n).">O(n)</abbr> | O(n). |
| `removeAt` | <abbr class="cx cx-linear" title="O(n).">O(n)</abbr> | O(n). |
| `take` | <abbr class="cx cx-linear" title="O(n).">O(n)</abbr> | O(n). |
| `drop` | <abbr class="cx cx-linear" title="O(n).">O(n)</abbr> | O(n). |
| `slice` | <abbr class="cx cx-linear" title="O(n).">O(n)</abbr> | O(n). |
| `splitAt(Predicate<? super T>)` | <abbr class="cx cx-linear" title="O(n).">O(n)</abbr> | O(n). |
| `splitAt(int)` | <abbr class="cx cx-linear" title="O(n).">O(n)</abbr> | O(n). |
| `reverse` | <abbr class="cx cx-linear" title="O(n).">O(n)</abbr> | O(n). |
| `sorted` | <abbr class="cx cx-linearithmic" title="O(n log n) comparisons.">O(n log n)</abbr> | O(n log n) comparisons. |
| `size` | <abbr class="cx cx-linear" title="O(n): the elements are counted. isEmpty is O(1).">O(n)</abbr> | O(n): the elements are counted. isEmpty is O(1). |
| `contains` | <abbr class="cx cx-linear" title="O(n): the elements are compared one by one until an equal one is found. The sets and the maps override it with a lookup.">O(n)</abbr> | O(n): the elements are compared one by one until an equal one is found. The sets and the maps override it with a lookup. |
| `indexOf` | <abbr class="cx cx-linear" title="O(n).">O(n)</abbr> | O(n). |
| `zip` | <abbr class="cx cx-linear" title="O(n); O(min(n, m)) for an argument of m elements when the pairing stops before the elements held at the back, which it must otherwise put in order. On a Queue built by enqueue, even a one-element argument costs O(n).">O(n)</abbr> | O(n); O(min(n, m)) for an argument of m elements when the pairing stops before the elements held at the back, which it must otherwise put in order. On a Queue built by enqueue, even a one-element argument costs O(n). |
| `sliding(int)` | <abbr class="cx cx-polynomial" title="O(n * size); each window is copied into its own Queue.">O(n * size)</abbr> | O(n * size); each window is copied into its own Queue. |
| `sliding(int, int)` | <abbr class="cx cx-polynomial" title="O(n + (n / step) * size): every element is walked, and each window is copied into its own Queue.">O(n + (n / step) * size)</abbr> | O(n + (n / step) * size): every element is walked, and each window is copied into its own Queue. |
| `grouped` | <abbr class="cx cx-linear" title="O(n); each block is copied into its own Queue.">O(n)</abbr> | O(n); each block is copied into its own Queue. |
| `distinct` | <abbr class="cx cx-linear" title="O(n).">O(n)</abbr> | O(n). |
